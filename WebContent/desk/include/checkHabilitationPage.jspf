<%

	if( !sessionUserHabilitation.isHabilitate(sPageUseCaseId ))
	{
		response.sendRedirect(response.encodeRedirectURL(rootPath 
				+ "desk/errorAdmin.jsp?idError=100&sUseCase=" + sPageUseCaseId ));
		return;
	}

%>
